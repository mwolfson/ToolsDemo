<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tools.demo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".ToolsDemoActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UIDemoActivity" />
        <activity android:name=".GoodUIDemoActivity" />
        <activity android:name=".MemDemoActivity" />
        <activity android:name=".ImagesDemoActivity" />
        <activity android:name=".LogcatDemoActivity" />
        <activity android:name=".FragmentHostActivity" />
        <activity android:name=".LocalizationDemoActivity" />
    </application>

</manifest>